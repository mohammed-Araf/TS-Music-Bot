permissions:
  enabled: true
  required_badges: []
  required_server_groups:
    - 50
  cache_badges: true
  cache_ttl_seconds: 5
  deny_message: "You are not allowed to use music commands."
  public_commands:
    - "np"
    - "q"
    - "src"
    - "ping"
