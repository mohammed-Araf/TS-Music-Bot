permissions:
  enabled: true
  required_badges:
    - "music_access"
    - "vip"
    - "admin"
  cache_badges: true
  cache_ttl_seconds: 300
  deny_message: "You are not allowed to use music commands."
  public_commands:
    - "np"
    - "q"
    - "src"
    - "ping"
